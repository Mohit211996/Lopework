<p id="notice"><%= notice %></p>

<h1>Request Connections</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @request_connections.each do |request_connection| %>
      <tr>
        <td><%= link_to 'Show', request_connection %></td>
        <td><%= link_to 'Edit', edit_request_connection_path(request_connection) %></td>
        <td><%= link_to 'Destroy', request_connection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Request Connection', new_request_connection_path %>
