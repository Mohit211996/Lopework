
<div class="container">

<%= render(partial: '/client/project_details', :locals => {:project => @project}) %>

<% if @project.project_status_id == 2%>

Under bidding

<% elsif @project.project_status_id == 1%>

<div class="row" style="padding: 10px;">
		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 h_bidding">
			Biddings 
			<%= render '/client_preferences/form',client_preference: @client_preference ,project_id: @project_id%>
		</div>
	</div>


<% @project.bids.each do|bid| %>

<%#= render(partial: '/client/startups', :locals => {:bid => bid}) %>



<div class="col-lg-3 col-md-6 col-sm-6 col-xs-6" style="padding: 10px;">
	<div class="border">
		
			<div class="startup_brief">
				<img src="/assets/client_1.jpg" class="client_pic">
				<p><%= bid.user.company_name %></p>
			</div>
			<div id="brief_<%= bid.user.id %>">
			<div class="startup_details" >
				<hr style="margin-top: 0px;">
				<p>Rating</p>
				<hr>
				<p>Experience : <%= bid.user.experience %></p>

				<hr>
				<%@technologies=bid.user.technologies.ids %>
				<%@technologies.each do|tech|%>
				<span><%= Technology.find(tech).technology_name%></span>
				<%end%>
				<hr>
				
			</div>
		</div>
		
		<div id="insta_<%= bid.user.id %>" style="display: none;">
			<%bid.installments.each do |insta|%>
				<div>
					<img src="/assets/money_icon.png" height="40px" width="40px" style="padding: 5px;">
		  			Rs.<%= insta.budget %>
		
		  			<img src="/assets/clock.png" height="40px" width="40px" style="padding: 5px;">
		  			<%= insta.time %> Weeks
		  			<br>
		  			
		  				<p style="word-break: break-word"><%= raw insta.details %></p>
		  			<hr>
		  				
		  			
				</div>
			<%end%>
		</div>
		<button class="pref" id="<%= bid.user.id %>">Preferences</button>
		<button class="" id="show_insta_<%= bid.user.id %>">Installments</button>
	</div>
</div>

<!-- <script type="text/javascript">
>>>>>>> b45ffd8801a33e509f8abf0c2b41045a752fba25
	$(document).ready(function(){
		var count_temp=0;
		$("#show_insta_<%= bid.user.id %>").click(function(){
			if(count_temp==0)
			{
				$("#brief_<%= bid.user.id %>").css("display","none");
				$("#insta_<%= bid.user.id %>").css("display","inline");
				count_temp++;	
			}
			else
			{
				$("#insta_<%= bid.user.id %>").css("display","none");
				$("#brief_<%= bid.user.id %>").css("display","inline");

				count_temp--;
			}
			
		});
	})
</script> -->

<%end%>
 
<% elsif @project.project_status_id == 3%>



<% elsif @project.project_status_id == 4%>
Completed
<%end%>

</div>


<script type="text/javascript">

	$(document).ready(function(){
		var id_array=[];
		var temp=0;
		var pos=0;
		var change=0;
		var count=0;
		$(".pref").click(function(){
			var id = $(this).attr('id');
			
			while(temp!=count)
			{
				if(id==id_array[temp])
				{
					change=1;
					pos=temp;
					break;
				}
				temp++;
			}
			if(change==1)
			{
				$(this).text("preferences");
				for(var i=temp;i<count;i++)
				{
					id_array[i]=id_array[i+1];
					var str1=id_array[i];
					var str2 ="#";
					var str3=str2.concat(str1);
					$(str3).text(i+1);
				}
				count--;
				change=0;
			}
			else
			{
				id_array[count]=id;
				count++;
				$(this).text(count);
			}
			temp=0;
		});

	




	$("#form").submit( function(eventObj) {

      $('<input />').attr('type', 'hidden')
          .attr('name', "user_id")
          .attr('value', id_array)
          .appendTo('#form');
      $('<input />').attr('type', 'hidden')
          .attr('name', "project_id")
          .attr('value', <%= @project.id%>)
          .appendTo('#form');
      $('<input />').attr('type', 'hidden')
          .attr('name', "startup_status_id")
          .attr('value', 3)
          .appendTo('#form');

          debugger
       

  	});





	})
</script>