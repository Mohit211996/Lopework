<%= form_with(model: connection, remote: true) do |form| %>
  <% if connection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(connection.errors.count, "error") %> prohibited this connection from being saved:</h2>

      <ul>
      <% connection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :selection, :value => selection %>
  <%= form.hidden_field :user_id, :value => client_id %>
  <%= form.hidden_field :startup_id, :value => startup_id%>

  <div class="actions ">
    <%= form.submit "#{text}",:id => "request_button_connection_#{selection}_#{client_id}",:class => "connection_request_button" %>
  </div>
<% end %>
